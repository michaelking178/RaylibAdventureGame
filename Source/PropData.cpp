#include "PropData.h"

PropData::PropData(const char* _spriteFilePath, Vector2 _spritePos, Vector2 _spriteSize):
    spriteFilePath(_spriteFilePath),
    spritePos(_spritePos),
    spriteSize(_spriteSize)
{}